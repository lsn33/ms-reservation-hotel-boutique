spring.application.name=reservas-service
server.port=8082

spring.config.import=optional:file:.env[.properties]

spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

spring.jpa.show-sql=true

# Mismo secreto que auth-service por ahora (HS256 compartido).
# Se reemplaza por la clave publica RSA en el siguiente paso.
jwt.secret=${JWT_SECRET:mi_secreto_super_largo_para_firmar_los_jwt_del_hotel_boutique_2026}
